An ordinary CLI tool that uses an IAM user to assume a Role using MFA to interact with KMS to encrypt and decrypt blobs. All ephemeral, volatile and stateless. You lost the file, you lost the content, but we have backups for that ;)
